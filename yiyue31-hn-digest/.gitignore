node_modules/
bun.lockb
package-lock.json

# 本地调试用 mock 评估脚本：读文件但输出硬编码 9.1/10 通过报告，不做真实评估。
# 仅用于调试 subagent 派发链路（路径/参数解析）；误入库会被当成真评估逻辑
# （49271994 帖的意外之声格式漂移正是被该 mock 的假通过报告掩盖的）。
scripts/subagent-eval.mjs
