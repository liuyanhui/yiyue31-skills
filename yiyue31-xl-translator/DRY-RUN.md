# 20KB 纸面干跑记录（tabletop walkthrough）

| | |
|---|---|
| **日期** | 2026-08-30 |
| **样例** | playbook 原文前 20KB（30 个标题行，其中 11 个在代码围栏内——正是当初碎成 53 片的同款文档） |
| **方式** | subagent 纸面推演 Step 0-10 + 3 个中断场景，不真跑 |
| **结论** | 主流程可走通、§5.1 命名经 classify() 推演发布安全成立；chunk 估算 = **2 片**（8.1 + 12.4KB，接缝恰在语义边界）；基线 ~27-28 次调用（等比于 64KB/~88） |

## 硬阻塞（2，已回灌 DESIGN）

1. **半块 sha 无文件系统根基**：报告头记"被审半块 sha1"但无切片规格，status/final-gate 无从重算 → 已冻结切片算法（见 DESIGN §5.1 补丁）
2. **阶段排序 vs 锚点/串行增强矛盾**：phase 推进下范文锚点与真实中文末段永远拿不到 → 已裁决**逐 chunk 流水**为执行序

## 需澄清（4，已回灌）

3. 40KB 阈值 vs M3 的 20KB 试跑矛盾 → Step 0 加 `--xl-force` 旁路
4. 分段器 fence 感知（11/30 伪标题在围栏内）→ M1a 验收项
5. re-keying 未覆盖 handoff/Step-2 产物 → 失效规则已补
6. 探针"路径无差别" vs probes/ 目录打架 → 探针报告落 reviews/，probes/ 只放样本输入

## 建议（4，已采纳 3）

7. 终检 FAIL 故障半径：单 chunk 根因走 scoped 重做（接缝复查+pm-review 重生成），全局作废仅限全局性根因 ✅
8. 审校者 glossary 改 per-chunk 投影（治 O(glossary) 膨胀）✅
9. slug 规则（首标题 3-4 词）+ 阈值缩放公式 ✅（公式 M1c 具体化）
10. 原文完整性预检 WARN（样例末句截断）✅

## 关键推演产物（备份）

- 样板审校报告头部（sha/model/time/unit/dim + 分节结论表）——schema 可执行性已验证
- 交接包 chunk-01 五件套骨架（map 摘要/译法投影/比喻台账/文风卡引用/串行追加段说明）
- B1 /compact 恢复的状态命令输出形态；B2 原文变更 re-keying 保留/作废清单；B3 终检 FAIL 回退链
